---
name: template-init
description: Retarget this bare-bones template to a real project. Use when the repository still describes itself as a template, or when the user asks to initialize, set up, name, or start a new project from it. Asks for a project name and stack, rewrites the identity files, prunes rules that do not apply, then deletes itself.
disable-model-invocation: true
---

# Retarget the template

The skill body lives at `.agents/skills/template-init/SKILL.md` — the location Codex
reads — so both agents run the same instructions from one file.

Read that file now and follow it.
